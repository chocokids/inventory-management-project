# 🎨 筛选器 UI 优化 - 可折叠设计

## 🎯 优化目标

让库存页面更简洁，筛选条件通过按钮控制显示/隐藏。

---

## ✨ 改进前后对比

### 改进前

```
☕️ 库存管理
───────────────────────────────
┌─────────────────────────────┐
│ 商品总数 │ 低库存 │ 筛选结果 │
└─────────────────────────────┘

┌─────────────────────────────┐ ← 始终显示
│ 🔍 筛选条件                  │
│ [全部][原料][耗材]...        │
│ ☑ 只显示低库存              │
└─────────────────────────────┘

[➕ 添加商品] [🧾 生成清单]
商品列表...
```

### 改进后

```
☕️ 库存管理
───────────────────────────────
┌─────────────────────────────┐
│ 商品总数 │ 低库存 │ 筛选结果 │
├─────────────────────────────┤
│ 🔍 筛选条件 [2] ▼           │ ← 点击展开/收起
└─────────────────────────────┘

[➕ 添加商品] [🧾 生成清单]
商品列表...


点击后展开:
┌─────────────────────────────┐
│ 🔍 筛选条件 [2] ▲           │
└─────────────────────────────┘
┌─────────────────────────────┐
│ [全部][原料][耗材]...        │
│ ☑ 只显示低库存              │
└─────────────────────────────┘
```

---

## 🎨 新设计特点

### 1. 折叠按钮

**位置：** 统计卡片底部

**外观：**
- 白色背景
- 咖啡色文字
- 左侧：🔍 图标 + "筛选条件" 文字
- 中间：活跃筛选数量徽章（红色圆点）
- 右侧：▲/▼ 箭头指示

**状态：**
- 收起：显示 ▼ 向下箭头
- 展开：显示 ▲ 向上箭头

### 2. 活跃筛选徽章

**显示条件：** 有任何筛选条件激活时

**显示内容：** 活跃筛选的数量

**示例：**
```
无筛选: 🔍 筛选条件 ▼
1个筛选: 🔍 筛选条件 [1] ▼  (如：只选了原料)
2个筛选: 🔍 筛选条件 [2] ▼  (如：原料 + 低库存)
```

### 3. 筛选面板

**默认状态：** 收起（隐藏）

**展开方式：** 点击折叠按钮

**关闭方式：** 
- 再次点击折叠按钮
- 或设置好筛选条件后自动收起（可选）

---

## 💡 使用方式

### 打开筛选面板

```
1. 点击 "🔍 筛选条件 ▼" 按钮
2. 筛选面板展开
3. 箭头变为 ▲
```

### 设置筛选条件

```
1. 打开筛选面板
2. 选择类别（如：原料）
3. 勾选/取消勾选 "低库存"
4. 筛选结果实时更新
5. 按钮显示活跃筛选数量 [2]
```

### 关闭筛选面板

```
1. 点击 "🔍 筛选条件 [2] ▲" 按钮
2. 筛选面板收起
3. 箭头变为 ▼
4. 筛选结果保持不变
```

### 清除筛选

```
方式一: 展开面板 → 点击 "清除筛选"
方式二: 展开面板 → 点击 "全部" + 取消勾选
```

---

## 🎯 优势分析

### UI 优势

✅ **页面更简洁** - 默认隐藏筛选器  
✅ **空间利用率高** - 不占用屏幕空间  
✅ **视觉层次清晰** - 主要信息更突出  
✅ **信息密度适中** - 避免信息过载

### 交互优势

✅ **一键展开/收起** - 操作简单  
✅ **状态清晰可见** - 徽章显示活跃筛选  
✅ **实时反馈** - 筛选结果即时更新  
✅ **灵活控制** - 想用就打开，不用就收起

### 用户体验

✅ **减少干扰** - 不需要筛选时不显示  
✅ **快速访问** - 需要时一键打开  
✅ **状态提示** - 徽章提醒有筛选激活  
✅ **节省滚动** - 页面更紧凑

---

## 📱 界面布局

### 收起状态（默认）

```
┌─────────────────────────────┐
│ 📊 统计卡片                  │
│ 商品总数 │ 低库存 │ 筛选结果 │
│   8件   │  5件  │   8件   │
├─────────────────────────────┤
│ 🔍 筛选条件 ▼               │
└─────────────────────────────┘

↓ 节省的空间 ↓

[➕ 添加商品] [🧾 生成清单]
商品列表...
```

### 展开状态（有筛选）

```
┌─────────────────────────────┐
│ 📊 统计卡片                  │
│ 商品总数 │ 低库存 │ 筛选结果 │
│   8件   │  5件  │   3件   │
├─────────────────────────────┤
│ 🔍 筛选条件 [2] ▲           │
└─────────────────────────────┘

┌─────────────────────────────┐
│ 商品类别                     │
│ [全部][●原料][耗材]...      │
│                             │
│ ☑ 🔴 只显示低库存商品        │
│                             │
│ 当前筛选: [原料][低库存]     │
│           [清除筛选]         │
└─────────────────────────────┘

[➕ 添加商品] [🧾 生成清单]
商品列表（已筛选）...
```

---

## 🔍 活跃筛选徽章

### 徽章逻辑

```typescript
活跃筛选数量 = 
  (类别不是"全部" ? 1 : 0) + 
  (勾选了低库存 ? 1 : 0)
```

### 显示示例

| 筛选条件 | 徽章显示 |
|---------|---------|
| 无筛选 | 不显示徽章 |
| 只选原料 | `[1]` |
| 只勾选低库存 | `[1]` |
| 原料 + 低库存 | `[2]` |

### 徽章样式

- 背景：红色 `bg-red-500`
- 文字：白色 `text-white`
- 形状：圆角胶囊 `rounded-full`
- 大小：小号 `text-xs`

---

## 💻 技术实现

### 状态管理

```typescript
// 控制筛选面板显示/隐藏
const [showFilters, setShowFilters] = useState(false);

// 判断是否有活跃筛选
const hasActiveFilters = () => {
  return filterCategory !== '全部' || filterLowStock;
};
```

### 折叠按钮

```tsx
<button
  onClick={() => setShowFilters(!showFilters)}
  className="w-full py-2 px-3 bg-white rounded-lg..."
>
  <span className="flex items-center gap-2">
    <span>🔍</span>
    <span>筛选条件</span>
    {hasActiveFilters() && (
      <span className="bg-red-500 text-white...">
        {(filterCategory !== '全部' ? 1 : 0) + (filterLowStock ? 1 : 0)}
      </span>
    )}
  </span>
  <span>{showFilters ? '▲' : '▼'}</span>
</button>
```

### 条件渲染

```tsx
{showFilters && (
  <Card className="mb-4">
    {/* 筛选面板内容 */}
  </Card>
)}
```

---

## 🎯 使用建议

### 推荐做法

✅ **默认收起** - 保持页面简洁  
✅ **需要时展开** - 设置筛选条件  
✅ **设置完收起** - 查看筛选结果  
✅ **观察徽章** - 记住有筛选激活

### 工作流程

```
日常盘点:
1. 打开库存页面（筛选面板收起）
2. 查看统计：商品总数 8件，低库存 5件
3. 点击展开筛选面板
4. 勾选 "低库存"
5. 点击收起面板
6. 查看 5 件低库存商品
7. 点击 "生成采购清单"
```

---

## 📊 空间节省

### 收起状态

- 筛选按钮高度：约 40px
- 节省空间：约 150px

### 展开状态

- 完整筛选面板：约 190px
- 按需显示，不影响浏览

---

## 🎨 视觉效果

### 折叠按钮

**正常状态：**
```
┌─────────────────────────────┐
│ 🔍 筛选条件 ▼               │
└─────────────────────────────┘
```

**悬停状态：**
```
┌─────────────────────────────┐
│ 🔍 筛选条件 ▼  (稍微变色)   │
└─────────────────────────────┘
```

**有筛选激活：**
```
┌─────────────────────────────┐
│ 🔍 筛选条件 [2] ▼           │
│         ↑                   │
│      红色徽章                │
└─────────────────────────────┘
```

---

## 🚀 立即体验

### 查看新设计

```bash
# 刷新浏览器
按 F5 或 Ctrl+R

# 进入库存页面
点击底部 "📦 库存" 标签
```

### 测试功能

1. **查看收起状态**
   - 默认筛选面板隐藏
   - 只显示统计卡片和折叠按钮

2. **展开筛选面板**
   - 点击 "🔍 筛选条件 ▼"
   - 面板滑出显示
   - 箭头变为 ▲

3. **设置筛选**
   - 选择类别
   - 勾选低库存
   - 观察徽章显示 [2]

4. **收起面板**
   - 点击 "🔍 筛选条件 [2] ▲"
   - 面板隐藏
   - 筛选结果保持

---

## 🎊 总结

### 改进要点

✅ **折叠设计** - 默认隐藏筛选面板  
✅ **一键切换** - 点击按钮展开/收起  
✅ **活跃徽章** - 显示筛选数量  
✅ **保持状态** - 收起后筛选继续有效  
✅ **页面简洁** - 节省屏幕空间

### 用户价值

🎯 **更简洁** - 页面信息密度降低  
🎯 **更高效** - 快速访问筛选功能  
🎯 **更清晰** - 徽章提醒有筛选  
🎯 **更灵活** - 按需展开收起

---

**页面更简洁，操作更高效！** ✨

立即刷新页面体验可折叠的筛选设计！




