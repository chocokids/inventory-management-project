# ⚡️ 快速启动指南

## 🚀 5 分钟快速开始

### 步骤 1: 安装依赖

```bash
npm install
```

### 步骤 2: 启动开发服务器

```bash
npm run dev
```

### 步骤 3: 打开浏览器

访问: `http://localhost:3000`

---

## 🎯 第一次使用

1. **自动初始化数据**
   - 系统会自动添加示例数据
   - 包含 8 个商品和 4 名员工
   
2. **探索功能**
   - 📦 **库存页面**: 查看和管理商品
   - 👥 **员工页面**: 查看和管理员工
   - 📋 **考勤页面**: 打卡记录与工资报告 ⭐
   - 📊 **报告页面**: 生成采购清单
   - ⚙️ **设置页面**: 系统设置

3. **生成采购清单**
   - 进入「库存」或「报告」页面
   - 点击「生成采购清单」按钮
   - Markdown 文件会自动下载

4. **生成工资报告** ⭐
   - 进入「考勤」页面
   - 选择统计月份
   - 点击「生成报告」按钮
   - 工资报告 Markdown 文件会自动下载

---

## 📱 在手机上使用

### 方法 1: 局域网访问

1. 启动开发服务器后，查看终端输出的 Network 地址
2. 在手机浏览器输入该地址（例如：`http://192.168.1.100:3000`）
3. 确保手机和电脑在同一局域网

### 方法 2: 部署后访问

```bash
# 构建生产版本
npm run build

# 部署 dist 文件夹到任意静态服务器
# 例如：Netlify, Vercel, GitHub Pages 等
```

---

## 🛠️ 常用命令

```bash
# 安装依赖
npm install

# 启动开发服务器 (http://localhost:3000)
npm run dev

# 构建生产版本
npm run build

# 预览生产版本
npm run preview

# 代码检查
npm run lint
```

---

## 💡 常见问题

### Q: 如何清除所有数据？
A: 进入「设置」页面 → 点击「清除所有数据」

### Q: 如何重新初始化示例数据？
A: 进入「设置」页面 → 点击「初始化示例数据」

### Q: 采购清单在哪里？
A: 点击生成后会自动下载到浏览器默认下载文件夹

### Q: 如何修改颜色主题？
A: 编辑 `tailwind.config.js` 文件中的 colors 配置

### Q: 数据存储在哪里？
A: 数据存储在浏览器的 IndexedDB 中，离线可用

---

## 🎨 自定义配置

### 修改端口号

编辑 `vite.config.ts`:

```typescript
server: {
  port: 3000, // 改为你想要的端口
}
```

### 修改配色

编辑 `tailwind.config.js`:

```javascript
colors: {
  cream: {
    50: '#你的颜色',
    // ...
  }
}
```

### 添加商品类别

编辑 `src/pages/InventoryPage.tsx` 中的 `categories` 数组

---

## 📦 项目依赖

核心依赖：
- `react` & `react-dom`: 前端框架
- `dexie` & `dexie-react-hooks`: IndexedDB 数据库
- `vite`: 构建工具
- `tailwindcss`: CSS 框架
- `typescript`: 类型系统

---

## 🌟 下一步

1. ✅ 启动项目
2. ✅ 探索功能
3. ✅ 添加你的商品和员工
4. ✅ 生成第一份采购清单
5. 🚀 根据需求自定义开发

---

**祝你使用愉快！ ☕️**

如有问题，请查看 [README.md](./README.md) 获取更多信息。

