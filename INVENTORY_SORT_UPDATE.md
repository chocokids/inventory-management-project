# 📦 库存管理排序优化 - 低库存商品优先显示

## 🎯 更新内容

在库存管理页面中添加了智能排序功能，**低库存商品将自动置顶显示**，方便及时关注和补货。

---

## ✨ 排序规则

### 三级排序逻辑

```
1. 首要优先级：低库存状态
   ├─ 低库存商品（quantity ≤ threshold）→ 置顶显示
   └─ 正常库存商品 → 显示在后面

2. 次要优先级（低库存商品内部）
   └─ 按库存量从低到高排序
      （最紧急的商品排在最前）

3. 第三优先级（正常库存商品内部）
   └─ 按商品名称字母顺序排序
      （方便查找）
```

---

## 📊 排序示例

### 优化前（无排序）
```
┌─────────────────────────┐
│ 咖啡豆 A      50kg  ✅  │
│ 牛奶          2L   ⚠️   │
│ 糖浆          15L  ✅   │
│ 咖啡杯        3个  ⚠️   │
│ 咖啡豆 B      30kg ✅   │
│ 吸管          1包  ⚠️   │
└─────────────────────────┘
```

### 优化后（低库存优先）
```
┌─────────────────────────┐
│ 吸管          1包  ⚠️   │ ← 最低库存
│ 牛奶          2L   ⚠️   │ ← 低库存
│ 咖啡杯        3个  ⚠️   │ ← 低库存
│ ─────────────────────   │
│ 咖啡豆 A      30kg ✅   │ ← 正常库存（按名称排序）
│ 咖啡豆 B      50kg ✅   │
│ 糖浆          15L  ✅   │
└─────────────────────────┘
```

---

## 🔧 技术实现

### 排序算法

```typescript
filtered.sort((a, b) => {
  const aIsLowStock = a.quantity <= a.threshold;
  const bIsLowStock = b.quantity <= b.threshold;
  
  // 1. 低库存商品排在前面
  if (aIsLowStock && !bIsLowStock) return -1;
  if (!aIsLowStock && bIsLowStock) return 1;
  
  // 2. 都是低库存时，按库存量从低到高排序
  if (aIsLowStock && bIsLowStock) {
    return a.quantity - b.quantity;
  }
  
  // 3. 都是正常库存时，按名称排序
  return a.name.localeCompare(b.name);
});
```

### 排序位置

排序在所有筛选条件应用之后执行：
```typescript
const applyFilters = () => {
  let filtered = [...inventory];
  
  // 1. 搜索关键字筛选
  // 2. 类别筛选
  // 3. 低库存筛选
  // 4. ✨ 排序（新增）
  
  setFilteredInventory(filtered);
};
```

---

## 📋 排序详解

### 1. 低库存优先

**规则：** `quantity <= threshold` 的商品排在最前

**示例：**
```
咖啡豆：50kg（阈值10kg）→ 正常库存
牛奶：  2L （阈值5L） → 低库存 ⚠️ 优先

结果：牛奶 排在 咖啡豆 前面
```

### 2. 低库存商品内部排序

**规则：** 库存量越少，排序越靠前

**示例：**
```
吸管：  1包 ⚠️  → 最紧急
牛奶：  2L  ⚠️  → 次紧急
咖啡杯：3个 ⚠️  → 较紧急

结果：吸管 > 牛奶 > 咖啡杯
```

### 3. 正常库存商品排序

**规则：** 按商品名称字母顺序（支持中文拼音）

**示例：**
```
咖啡豆 B  50kg ✅
糖浆      15L  ✅
咖啡豆 A  30kg ✅

结果：咖啡豆 A > 咖啡豆 B > 糖浆
```

---

## 🎨 视觉效果

### 列表显示

```
库存管理
━━━━━━━━━━━━━━━━━━━━━━

⚠️ 低库存商品（需要补货）
┌─────────────────────────┐
│ 🔴 吸管                  │
│    1包 / 阈值: 5包       │
│    [编辑] [删除]         │
└─────────────────────────┘

┌─────────────────────────┐
│ 🔴 牛奶                  │
│    2L / 阈值: 5L         │
│    [编辑] [删除]         │
└─────────────────────────┘

━━━━━━━━━━━━━━━━━━━━━━

✅ 正常库存商品
┌─────────────────────────┐
│ 咖啡豆 A                 │
│    30kg / 阈值: 10kg     │
│    [编辑] [删除]         │
└─────────────────────────┘

┌─────────────────────────┐
│ 咖啡豆 B                 │
│    50kg / 阈值: 10kg     │
│    [编辑] [删除]         │
└─────────────────────────┘
```

---

## 🚀 使用场景

### 1. **日常查看**
打开库存管理页面，立即看到需要补货的商品

### 2. **快速补货**
最紧急的商品在最上方，优先处理

### 3. **生成采购单**
低库存商品已经按紧急程度排序，便于制定采购计划

### 4. **搜索和筛选**
即使在搜索或筛选后，低库存商品仍然优先显示

---

## ✅ 优势

### 提高效率
- ✅ **无需滚动**：重要商品直接在顶部
- ✅ **快速决策**：按紧急程度排序
- ✅ **节省时间**：不用查找低库存商品

### 减少错误
- ✅ **避免遗漏**：低库存商品醒目置顶
- ✅ **优先处理**：最紧急的商品最先看到
- ✅ **及时补货**：降低缺货风险

### 用户体验
- ✅ **直观明了**：重要信息优先呈现
- ✅ **符合习惯**：紧急事项在前
- ✅ **智能排序**：自动化管理

---

## 📊 排序场景

### 场景 1：全部商品
```
显示顺序：
1. 低库存商品（按库存量排序）
2. 正常库存商品（按名称排序）
```

### 场景 2：搜索 "咖啡"
```
显示顺序：
1. 包含"咖啡"的低库存商品
2. 包含"咖啡"的正常库存商品
```

### 场景 3：筛选 "原料" 类别
```
显示顺序：
1. 原料类别中的低库存商品
2. 原料类别中的正常库存商品
```

### 场景 4：仅显示低库存
```
显示顺序：
所有低库存商品按库存量从低到高排序
```

---

## 🔄 与其他功能的配合

### 搜索功能
```
搜索 → 筛选 → 排序（低库存优先）
```

### 类别筛选
```
类别筛选 → 排序（低库存优先）
```

### 低库存筛选
```
低库存筛选 → 排序（按库存量）
```

### 统计卡片
```
显示低库存总数 + 列表按优先级排序
```

---

## 💡 实际应用

### 早上查看库存
```
打开应用 → 首先看到需要补货的商品
          → 制定今日采购计划
```

### 生成采购清单
```
点击"生成采购清单" → 自动包含低库存商品
                    → 已按紧急程度排序
```

### 添加新商品后
```
添加商品 → 如果是低库存 → 自动排在前面
         → 如果是正常库存 → 按名称排序插入
```

---

## 📝 代码变更

### 修改文件
- `src/pages/InventoryPage.tsx`

### 修改内容
- 在 `applyFilters()` 函数中添加排序逻辑
- 排序在所有筛选条件之后执行
- 使用三级排序算法

### 代码行数
- 添加约 15 行排序代码
- 不影响现有功能
- 完全向后兼容

---

## 🎊 总结

### 新增功能
- ✅ 低库存商品自动置顶
- ✅ 低库存商品内部按库存量排序
- ✅ 正常库存商品按名称排序
- ✅ 与搜索和筛选功能完美配合

### 用户收益
- 🎯 **更高效**：重要信息优先呈现
- 🛡️ **更安全**：降低缺货风险
- 💡 **更智能**：自动化排序管理
- 😊 **更友好**：符合使用习惯

### 技术亮点
- 🔧 **简洁高效**：15 行代码实现
- 🎨 **用户友好**：零学习成本
- 🚀 **性能优异**：原生 Array.sort()
- 🔄 **可扩展**：易于调整排序规则

---

**立即刷新页面，体验智能排序功能！** 🚀

低库存商品将自动显示在列表顶部，帮助您更高效地管理库存！



